# Sessie 30 jun 2026 — VibeKilled.rip

## Wat is deze sessie gedaan

### Durable anonieme identiteit (cookie-anker) — "anoniem profiel bouwt nu op"

**Probleem (Thomas):** elke keer terugkomen + een paar acties = registreert als een andere naam.
Het anonieme profiel bouwde nooit op.

**Root cause:** identiteit zat *alleen* in localStorage (`vk:userId` + `vk:name`).
localStorage wordt gewist door Safari/iOS ITP (~7d), private mode, of cross-origin/andere
browser. Bij elke wipe mintte `getUserId()` een vers id en `getName()` een verse random
naam → nieuw "persoon" → server-side profiel (kills, medals, score, streak — alles aan
`userId` in Redis) accumuleerde nooit.

**Fix — server-set, durable first-party cookie + localStorage-mirror:**

- **`src/proxy.ts`** (nieuw — Next 16 hernoemde `middleware.ts` → `proxy.ts`, `export function proxy`):
  zet bij elk request `vk_uid` als server-`Set-Cookie` (Max-Age ~400d, sliding, `SameSite=Lax`,
  `Secure` alleen op https, `httpOnly:false` zodat client 'm leest). Bestaat de cookie al →
  alleen expiry verversen; nieuw device → één keer een vers `u_…` id (`crypto.randomUUID()`).
  Server-`Set-Cookie` overleeft ITP wél (anders dan localStorage én client `document.cookie`).
  Matcher: alle routes behalve `_next/static|_next/image|favicon.ico`.
- **`src/lib/identity.ts`**:
  - `getUserId()` = **localStorage-first → cookie → mint**, mirrort altijd naar **beide**.
    localStorage-first houdt bestaande profielen intact; cookie is de durable fallback.
  - `getName()` leidt default-naam **deterministisch** af uit het id (`devName(seedFromId(uid))`)
    → wiped naam-opslag geeft exact dezelfde alias terug. Custom naam blijft override.
  - `setName()` schrijft nu ook naar de cookie. Nieuwe helpers: `readCookie`/`writeCookie`/
    `seedFromId`, `COOKIES = { uid: "vk_uid", name: "vk_name" }` (cookie-naam mag geen ":").

**Server-side: niks gewijzigd** — profiel hing al volledig aan `userId` in Redis, dus het
persisteert nu automatisch zodra het id durable is.

## Beslissingen / notes
- Aanpak gekozen: **server-cookie + proxy** (durabel op Safari/iOS) i.p.v. alleen client dual-write
  (een client-set cookie wordt óók door ITP na ~7d gecapt).
- **Geen** canonical redirect: Thomas gebruikt in praktijk alleen `vibekilled.rip` (één origin).
- Bestaande users op Thomas' device verliezen niks: localStorage-first houdt het oude id vast en
  corrigeert de cookie ernaar op dezelfde load.
- AGENTS.md gevolgd: eerst Next 16 proxy-docs gelezen (`node_modules/next/dist/docs/...proxy.md`)
  vóór code — `middleware` → `proxy` was precies de breaking change.

## Status einde sessie
- ✅ `tsc --noEmit` schoon · ESLint schoon op beide files.
- ✅ Gecommit & gepusht naar `main`.
- ✅ `vercel --prod` gedeployd.

## Hoe testen (na deploy)
- DevTools ▸ Application ▸ Cookies: `vk_uid` staat er en blijft.
- Doe acties → **wis localStorage** (cookies laten staan) → reload → zelfde naam + profiel terug
  (hersteld uit cookie). Dat bewijst de fix.

## Volgende stappen / opties
- [ ] Op echt iOS-device bevestigen dat het profiel over dagen heen blijft (ITP-proof).
- [ ] Optioneel: server-side de `vk_uid`-cookie als fallback voor body-`userId` in API-routes
      (extra robuust als een client ooit een lege userId stuurt) — nu bewust niet gedaan (scope).

---

# Sessie 30 jun 2026 — sessie 2: Campfire → arcade + StartupBar

## Wat is deze sessie gedaan

### 1. StartupBar.co directory-widget in de `<head>`
- Doel: VibeKilled in de StartupBar-directory; hun "verify it's live"-check moet de loader-script vinden.
- **`src/app/layout.tsx`**: een plain `<script async src="https://startupbar.co/widget/loader.js" data-startup-id="5273a5d1-…">` in de body. React 19 **hoist** een async-script met `src` als *literal* tag in de SSR-`<head>` (data-attributen behouden).
- Waarom geen `next/script` `beforeInteractive`: die injecteert via een `__next_s`-bootstrap-array i.p.v. een literal `<script>`-tag. URL+id zitten dan wél in de HTML (substring-check ok) maar een DOM-parsende verifier ziet geen echt script-element. Plain-tag = robuuster.
- **Geverifieerd**: na `next build` staat de letterlijke `<script … src="…loader.js" data-startup-id="…"></script>` ín `<head>` van `.next/server/app/index.html` (offset-check: tussen `<head>` en `</head>`).
- Thomas doet ná deploy: "Check now" → groen → **Pay $9.99 & submit**.

### 2. Campfire = arcade (chat eruit, Tetris + LOLReads erin)
Thomas' opdracht: haal de chat uit de campfire-room, zet er een **volledig speelbare Tetris** + **LOLReads**, en laat in de admin zien dat iemand Tetris speelde.

**`src/components/Tetris.tsx` (nieuw) — zelfstandige, volledig speelbare Tetris:**
- Pure helpers buiten de component: 10×20 grid, 7-bag randomizer (`shuffle`/`drawFromBag`), matrix-rotatie (`rotateCW`) + **wall-kicks** (`[0,-1,1,-2,2]`), `collides`, `lockPiece`, `clearLines`, ghost-drop, level-speed-curve, NES-achtige line-scores.
- Game-state in één `useRef` (`Game`) zodat de **rAF-loop nooit stale closures leest**; render-snapshot wordt alleen naar `useState` gepubliceerd (`commit()`) bij een zichtbare verandering → geen per-frame React-churn. Init-truc: `useState(freshGame)` levert één game, gedeeld door ref én eerste view (geen ref-read tijdens render → schoon op de strenge `react-hooks/refs`-regel).
- Controls: toetsenbord (← → bewegen, ↑/x roteren, ↓ soft-drop, space hard-drop, p pauze, Esc sluit) **én** on-screen touch-knoppen (werkt op mobiel; campfire leeft in een Leaflet-popup).
- Overlay via `createPortal(document.body)` — escapet de popup-transform, vult de viewport. `onPlayed`-callback vuurt **precies één keer** per game (game-over óf unmount na min. 1 lock).

**`src/components/Campfire.tsx` (herschreven):** alle chat-logica weg (`useChat`/`sendChat`/messages/welcome/input). Behoudt join-beat, presence-beat (`joinCampfire`) + countdown-header. Body = room-menu: een **Tetris-tile** (opent overlay) + `<LolReads />`. Resurrectie → component valt in de "fire's out"-branch → `<Tetris>` unmount → cleanup rapporteert de game. `onTetrisPlayed` → `reportTetris(...)`.

**`src/components/LolReads.tsx`:** overlay nu ook `createPortal(document.body)` (boy-scout: anders staat 'ie scheef in de getransformeerde Leaflet-popup waar de campfire 'm rendert).

**Backend (admin ziet Tetris):**
- `src/lib/types.ts`: `"tetris"` toegevoegd aan `AdminEventType`.
- `src/lib/store.ts`: keys `tetrisPlays` + `tetrisHigh`; **`recordTetris()`** — gated op een **actieve pin** (zelfde muur die de campfire vereist = anti-abuse), bumpt play-counter + high score, `adminLog({type:"tetris", text:"scored N · M lines"})`. `AdminStats` + `getAdminStats()` geven `tetrisPlays`/`tetrisHigh` mee.
- `src/app/api/tetris/route.ts` (nieuw): `POST` met rate-limit + clamps, valideert provider, roept `recordTetris`.
- `src/lib/api.ts`: `reportTetris()` (fire-and-forget).
- `src/app/admin/page.tsx`: `EVENT_META.tetris` (🎮 "played Tetris"), twee metric-tiles **🎮 tetris plays** + **🏆 tetris high**, grid 6→4 koloms, label "live in chat" → "🔥 around the fire" (chat bestaat niet meer).

## Beslissingen / notes
- AGENTS.md gevolgd: Next 16 `next/script`-docs gelezen vóór de head-injectie; ontdekt dat App-Router `beforeInteractive` ≠ literal tag → bewust naar React-19-script-hoisting geschakeld.
- **Next 16 draait géén `next lint` meer in `next build`** (apart CLI-commando). Pre-existing `react-hooks`-errors in `admin/page.tsx` blokkeren de build dus niet; m'n eigen files zijn schoon gehouden.
- Tetris-render gekozen als CSS-grid van divs (geen canvas) — geen DPI-gedoe, en renders alleen bij echte verandering, dus prima performance.

## Status einde sessie
- ✅ `tsc --noEmit` schoon · ESLint schoon op alle nieuwe/aangeraakte files.
- ✅ `next build` (Turbopack) groen; `/api/tetris` geregistreerd; StartupBar-tag bevestigd in `<head>`.
- ✅ Gecommit & gepusht naar `main` (op Thomas' verzoek "push maar meteen").
- ⏳ Vercel-deploy + StartupBar "Check now" doet Thomas.

## Hoe testen (na deploy)
- Drop een pin → open je eigen kaartje → Campfire toont **🎮 Tetris** + **📚 LOLReads** (geen chat).
- Speel Tetris (toetsenbord op desktop, knoppen op mobiel) → game-over of sluit → `/admin` toont de play in de journey ("🎮 played Tetris · scored N") + **🎮 tetris plays**/**🏆 high** lopen op.
- StartupBar: refresh hun stap-2-pagina → "Check now" → groen.

---

# Sessie 1 jul 2026 — sessie 3: instant eigen-card, notificaties, opruimen

Veel iteratie o.b.v. Thomas' feedback (kort samengevat, nieuwste bovenaan):

### Eigen card opent INSTANT (de grote frustratie)
- Symptoom: na drop kwam de eigen card (map-popup) niet / niet meteen; "random open kaart" won.
- Fixes onderweg: pin optimistisch in `["pins"]` + `["pin",id]` cache (marker mount direct); `FocusController` sluit eerst andere popups; live-casualty-fly onderdrukt zolang jíj down bent (je blijft op je card; kijkers zónder pin vliegen wél mee).
- **Kern-fix (met GPT-5.2 overlegd):** de eigen pin **opent z'n eigen popup van binnenuit** in `PinMarker` (`useEffect` → `marker.openPopup()` op mount). Tegen die tijd heeft react-leaflet de layer toegevoegd + popup gebonden (child-effects eerst), dus het lukt altijd; `autoPan` brengt 'm in beeld. Geen externe ref/timing-race meer. Popup krijgt `max-height:70vh; overflow:auto` zodat een lange card niet wordt afgekapt.
- Arcade (Tetris + LOLReads) staat nu **bovenaan** de eigen card, **enkel** daar (uit de SessionPanel gehaald — Thomas wilde 'm niet rechts).

### Reactie-notificaties (sympathy/Good4U/🤝)
- `notify.ts` `fireReaction(kind)` (per-kind tag, alleen in achtergrond-tab). `ReactionNotify` (page-root, altijd gemount, overleeft dichte mobiele sheet) detecteert count-stijging op je pin. `useMyPin` → `refetchIntervalInBackground:true` zodat het óók vuurt als je weg-getabt bent. LandingNotify-copy kloppend gemaakt (was: dode chat-belofte).

### "X also behind the wall"-badge
- Op elke card: persoon-**SVG** (geen emoji) + `animate-ping` pulse + live `stats.active`.

### Admin + leaderboard
- Journey toont nu **geven-land → ontvangst-land** (`actorCountry → country`, `🇳🇱 → 🇧🇷`).
- **Tetris high-score per dev** op de Vibe Kings-leaderboard (public + admin): `recordTetris` bewaart per-user best (`vk:stat:tetris:userhigh`), `getLeaderboard` joint 'm in.

### Opruimen (fix-sweep)
- **StartupBar-widget verwijderd** (Thomas wilde 'm eruit).
- **Alle dode chat-code weg**: routes `/api/chat` + `/api/admin/chat` (`git rm`), store (`postChat`/`deleteChatMessage`/`getChat`/`K.chat`/`CHAT_MAX`/`AdminStats.chat`), `api.ts` (`fetchChat`/`sendChat`), `hooks` (`useChat`), `types` (`ChatMessage`, `"chat"`-event), admin "seed as a dev"-kaart. **Behouden:** `joinCampfire`/`chatPresence`/`chatLiveCount` → "around the fire"-presence.

## Status einde sessie
- ✅ `tsc` schoon, `next build` groen bij elke stap. Meerdere commits + `vercel --prod` deploys, allemaal gealiased naar vibekilled.rip.
- ⏳ Thomas test op echte devices (mobiel + achtergrond-tab notificaties).

## Volgende stappen / opties
- [ ] Mobiel: Tetris-touch + board-grootte in de popup, LOLReads-overlay, notificaties op device.
- [ ] Fase 2: Web Push (service worker + VAPID) voor closed-tab notificaties.
- [ ] Evt. 2e arcade-tile ("Burn The Tokens" idle-clicker), Tetris hold-piece.
